<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.alexandre.investmentportfolio">

    <uses-permission-sdk-23 android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InvestmentPortfolioApp">
        <activity
            android:name=".activity.FormEditItemActivity"
            android:label="@string/title_activity_item_edit_list" />
        <activity
            android:name=".activity.FormCreateItemActivity"
            android:label="@string/title_activity_item_create_list" />
        <activity
            android:name=".activity.ListItemsActivity"
            android:label="@string/title_activity_list"
            android:theme="@style/Theme.InvestmentPortfolioApp.NoActionBar" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>